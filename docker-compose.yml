services:
  server:
    build: ./server
    ports:
      - "3500:3500"
    environment:
      - ORIGIN=http://localhost:5173
    volumes:
      - files_data:/app/files

  client:
    build: ./client
    ports:
      - "5173:80"

volumes:
  files_data:
